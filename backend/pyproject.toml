[project]
name = "ics-hrms-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "alembic",
    "cryptography",
    "fastapi",
    "pymysql",
    "python-multipart",
    "sqlalchemy",
    "uvicorn[standard]",
]
